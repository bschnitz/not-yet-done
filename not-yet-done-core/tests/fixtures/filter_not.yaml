# not-yet-done-core/tests/fixtures/filter_not.yaml

description: "NOT inverts a condition"

filter:
  not:
    [deleted, =, true]

expected:
  - "Gustav works on the parser"
  - "Augustava reviews the PR"
  - "Fix the login bug"
  - "Write release notes"
  - "High priority cancelled task"
  - "Child task with parent"
