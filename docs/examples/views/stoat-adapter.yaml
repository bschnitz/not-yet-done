# Example: Stoat (Revolt-fork chat) adapter credentials
#
# Place in: ~/.config/not_yet_done/views/stoat-adapter.yaml
# Referenced by stoat.yaml via: adapter.config: stoat-adapter.yaml
#
# Stoat is a chat platform (fork of Revolt). It is the first *streaming*
# adapter: the server list and live updates arrive push-only over a
# WebSocket. You only configure the base domain here — the API path
# (`/api`) and the WebSocket URL are self-discovered via `GET /api/`.

url: https://chat.example.org # base domain only; /api and /ws are discovered
name: chat # optional human-readable label

auth:
  # Stoat logs in by email + password against
  # `POST /api/auth/session/login`, which returns a session token sent as
  # the `X-Session-Token` header on every request (and to the gateway).
  #
  # NOTE on field names: Stoat's `password-login` mechanism declares the
  # fields `username` + `password`. Stoat has no separate username login,
  # so `username` carries your **email address** — which is why the login
  # prompt labels it "E-mail address". Only the session token is ever
  # persisted — never the password (session_cache defaults to
  # `until-rejected`: kept until the server returns 401/403, then a fresh
  # login is triggered).
  #
  # `nyd config auth stoat` prints the mechanism, its fields and every
  # provider; `nyd config build stoat` asks and writes the block.
  mechanism: password-login
  bindings:
    - field: username # ← put your LOGIN EMAIL here (mechanism-declared name)
      provider:
        type: prompt
        # Optionally pre-fill the email so only the password is asked for:
        # prefill: me@example.org
        #
        # Or read it non-interactively from the environment / a secret
        # manager instead of prompting:
        # type: env
        # var: STOAT_EMAIL
    - field: password
      provider:
        type: prompt
        # Non-interactive alternatives (same as any password-login adapter):
        # type: command
        # script: pass show chat/example/password
        # timeout_secs: 30

# Multi-factor auth (MFA) is NOT supported yet: if your account requires
# an MFA ticket the login fails with a clear message. Use an account
# without MFA for now (see plan §9 / docs).

# Optional override for the backing store (session token + view sort
# state only — chat state is never cached to disk). Sea-orm-compatible
# URL; falls back to a private SQLite file under the user's local data
# directory when omitted.
# db:
#   url: "sqlite:///home/me/.local/share/not_yet_done/stoat.sqlite?mode=rwc"

# How a request that produced no answer at all is sent again — the connection
# refused, the handshake lost, the answer never arriving. Such a failure says
# nothing about the server a moment later, so the adapter repeats the call
# instead of handing the error straight to the user.
#
# `attempts` counts the first try; `1` switches repeating off. `backoff_ms` is
# the wait before the second attempt and doubles before each further one.
# Defaults: 2 attempts, 250 ms.
#
# Only calls without side effects are repeated after a *timeout*, because a
# timed-out request may well have arrived and run — repeating a message there
# would send it twice. When the connection never came up, every call is
# repeated: nothing reached the server, so nothing can be doubled.
# retry:
#   attempts: 2
#   backoff_ms: 250
