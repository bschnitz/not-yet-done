/target
/nyd.db
ignored
nyd
nyd-tui
prompt*.md
todo.md
/not-yet-done-ratatui/target/
/not-yet-done-ratatui/tui-realm/
.memsearch/

# CodeGraphContext per-repo index (local, machine-specific)
/.codegraphcontext/
# MCP server config holds a machine-specific absolute path
/.mcp.json

# Privacy-cleanup working area (candidate lists, status, plan — never committed)
/cleanup/

# Playwright MCP scratch output (may contain live session data — never commit)
.playwright-mcp/

# Ad-hoc screenshots dropped in the repo root (e.g. Azure app-registration
# API-permission captures) — contain live tenant data, never commit.
/*.png
